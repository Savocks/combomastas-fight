import { Typography } from '@mui/material';

function AppBarTitle() {
  return (
    <Typography typography="h5" letterSpacing={[10, 14]} color="primary">
      DREAMER & VISIONARY
    </Typography>
  );
}

export default AppBarTitle;
