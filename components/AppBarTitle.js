import { Typography } from '@mui/material';

function AppBarTitle() {
  return (
    <Typography typography="h5" letterSpacing={[2, 12]} color="primary">
      DREAMER & VISIONARY
    </Typography>
  );
}

export default AppBarTitle;
